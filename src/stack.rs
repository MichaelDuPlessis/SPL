struct Stack<T> {
    vec: Vec<T>,
    pos: usize,
}

impl<T> Stack<T> {
    fn new() -> Self {
        Self {
            vec: Vec::new(),
            pos: 0,
        }
    }

    fn push(element: T) {
        
    }
}